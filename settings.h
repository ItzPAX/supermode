#pragma once
#include <string>

/// only use if EAC encrypts the DTB in EPROCESS
/// requires you to start supermode.exe while the game is running
static constexpr bool EACCR3 = false;

static std::string LOCAL_NAME = "supermode_me.exe";
static std::string TARGET_NAME = "cs2.exe";